import type { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.easycashpk.bazaardaketee',
  appName: 'Bazaar Daketee',
  webDir: 'dist',
};

export default config;
